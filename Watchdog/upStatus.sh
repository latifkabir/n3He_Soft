#!/bin/bash

# upStatus()
# {
    ps -e | grep -w 'n3he'
    # STATUS=$?
    # echo $STATUS > upStatus.txt

    # ps -e | grep -w 'okular'
    # STATUS=$?
    # echo $STATUS
    # echo $STATUS > upStatus.txt

    # If n3he is running then STATUS is 0 (exit Success) , if not running the STATUS is 1
# }

# upStatus
