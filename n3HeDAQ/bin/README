1. After doing 'make' the binary file is produced inside this directory.
2. Copy the .desktop file found here on your Desktop so that you can launch the gui mode by just double clicking on it.
3. After copying modify the .desktop file so that it is linked to the binary/executable file ,also the image for the icon correctly.
